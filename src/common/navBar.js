const menus = ["Configuration"];

const subMenus = {
    Configuration: [
        {
            path: "Configuration",
            lable: "Confgiuration",
        }
    ]
}


export { menus, subMenus }